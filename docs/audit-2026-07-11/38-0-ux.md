# 38-0 End-to-End UX Audit — Findings

## P0 — Critical

**1. The core win reward is broken: "SWAP ONE PLAYER →" is a dead-end loop.**
`swapAvailable` is never set to `true` anywhere in the codebase — `recordWin()` no longer grants it (`src/lib/draft/local.ts:69-71`; the only assignments are `false` at local.ts:60,199 and swap consumption at `src/app/38-0/swap/page.tsx:74`). Yet the match-result screen's primary post-win CTA is `SWAP ONE PLAYER →` → `/38-0/swap` (`src/app/38-0/match/result/page.tsx:134-137`), and the swap page's guard `if (!t.swapAvailable) router.replace("/38-0/team")` (`swap/page.tsx:37`) silently bounces every user back to the team page. The advertised win→swap dopamine loop (still described in YOURSCORE.md §5B) literally cannot fire; the team page's earned-swap banner (`team/page.tsx:478`) is dead UI. **Fix:** either have `recordWin()` set `swapAvailable: true` again, or remove the CTA/banner and the orphaned `/38-0/swap` page, and update YOURSCORE.md.

## P1 — High

**2. Guests have no Quick Match — the documented guest loop stops at the season sim.**
`quickMatch()` is only reachable via the "Practice vs CPU" button, which renders exclusively for signed-in users; guests get "Fresh team" instead (`src/app/38-0/team/page.tsx:523-531`). No other entry point exists. YOURSCORE.md §5B claims "guests get the full draft + Quick Match loop" (✅ Live). A guest's entire loop is draft → season sim → sign-up wall — no match, no shootout, no opponent ever. **Fix:** restore a guest-accessible Quick Match (it's fully local — `quickMatch()` needs no auth), or consciously accept season-sim-only for guests and fix the doc.

**3. Forced ~95-second unskippable match playback after every Quick Match.**
`/38-0/match/watch` plays 45s + 5s halftime + 45s (`WATCH_CONFIG.halfSeconds: 45`, `src/lib/draft/playback.ts:18`; `watch/page.tsx:33-56`) with **no skip control anywhere** — the season sim has "Skip →" (`season/page.tsx:179`) but the match watch has none. For a solo CPU practice match it's pure friction on the highest-frequency loop. **Fix:** add a persistent "Skip to result →" on `/38-0/match/watch` (result is already resolved in localStorage before playback starts).

**4. Draft placement is irreversible with no undo and no fit guidance.**
`placePlayer` only fills open slots and there is no remove/undo during the draft (`clearSlot` is used only by the dead swap flow, `local.ts:148-150`). The placement panel colours slot chips by line category but never shows fit quality (`play/page.tsx:180-190`), while `fitMultiplier` penalises off-position placement in Strength; the amber "cover" warning appears on the Pitch only **after** the irreversible tap (`Pitch.tsx:13-18`). `bestOpenSlot()` exists but is used only by the bot drafter (`opponent.ts:80,129,156`) — humans get no "best slot" hint. **Fix:** mark the best-fit slot chip (e.g. "★ natural"), show fit colour on the chip itself, allow re-placing the last pick before the next spin.

**5. Loss messaging still sells the retired "stale team" model.**
Stale status was deliberately retired (`local.ts:230-231` clears it; `recordLoss` keeps the team active, local.ts:74-76), but the loss CTA is a red `REBUILD XI →` linking to `/38-0` (result/page.tsx:142-145) and the loss share copy says "Rebuilding…" (result/page.tsx:45). YOURSCORE.md §5B still says "lose → team stale → rebuild". The player is told to scrap a perfectly playable XI. **Fix:** make the loss CTA "GO AGAIN →" (team page) with "Build a fresh XI" secondary; update doc + share copy.

**6. Four different leaderboards with colliding names.**
Within 38-0 alone: hub tab "Leaderboard ✓" = verified season records (`page.tsx:103,357`), pill "⚔️ H2H Ladder" = `/38-0/leaderboard` daily/all-time H2H points, pill "🏅 WC Season" = `/38-0/wc/board` 8-0-0 board, plus per-league boards — and the shared `DraftHeader` sub-nav calls the H2H ladder "🏆 Board" (`DraftHeader.tsx:16-21`), the exact word the hub uses for the *verified* board. Two competing sub-navs for the same section. Points semantics (W3/D1 H2H pts vs season W-D-L vs WC 8-0-0) never explained side-by-side. **Fix:** one canonical sub-nav; distinct nouns ("H2H Ladder" / "Season Records ✓" / "WC Board") everywhere; a one-line "what counts here" caption per board.

## P2 — Medium

**7. `/38-0/match/prematch` is an orphaned route — `saveMatchup()` has no callers.**
The pre-match "swap up to 3 players" screen (`prematch/page.tsx`) requires `loadMatchup()`, but `saveMatchup` is defined (`local.ts:344`) and never invoked, so the page always instantly redirects to `/38-0/team` (prematch/page.tsx:46). The team page comment (team/page.tsx:170-172) still describes routing through it. **Fix:** delete the route + `Matchup` plumbing or rewire the async-challenge flow through it.

**8. League draft tabs have zero onboarding — "How it works" exists only for World Cup.**
PL/La Liga tabs get one blurb line (`page.tsx:32`); the collapsible "ⓘ How it works" panel exists only under the WC tab (page.tsx:307-350). The core concept — Strength → projected season → chase 38-0 — is never stated before the player invests 33 taps. Expert mode's 🔒 icon (page.tsx:268) reads as "locked/premium" when it means "ratings hidden". **Fix:** add the collapsed how-it-works to league tabs; replace 🔒 with 🙈/"ratings hidden".

**9. Post-season giveaway modal hijacks the result moment 700ms after reveal.**
On every season completion (and every live-H2H result), the £25 giveaway sheet auto-opens over the scorecard (`season/page.tsx:125-134`, `live/match/[id]/page.tsx:444-451`) before the player has read their own record. Trains dismissal and cheapens the reveal. **Fix:** keep the always-visible WIN £25 card, drop the auto-open (or once per day max).

**10. Fabricated "managers online" counter in the matchmaking spinner.**
`FindingPanel` seeds a random 70–220 "managers online" figure and jitters it on a timer (`live/page.tsx:373-377`). A concrete fake liveness claim adjacent to the Versus tab's flagged `TODO(real-presence)`. Trust in "verified ✓" boards is collateral damage if noticed. **Fix:** back it with the real presence metric from `/api/versus/activity`, or soften to non-numeric copy.

**11. Challenge accept silently bounces users who don't have a complete XI.**
On `/38-0/challenge/[code]`, tapping "ACCEPT WITH MY XI ⚔️" with no complete local team does `router.push("/38-0")` with no message (`challenge/[code]/page.tsx:50-51`) — the invitee (top of the viral funnel) loses the challenge context entirely. **Fix:** route to `/38-0/play` with a "build your XI to accept" banner and return to the challenge after completion (the WC flow already does this with `saveDraft`/`pendingEnter`, wc/page.tsx:44-50).

**12. Live H2H penalties: opponent's kicks never animate; no dive-role explanation.**
In live H2H pens the opponent's kicks are scoreboard-only (`PenaltyShootout.tsx:177`) and `onDive` is a no-op (`live/match/[id]/page.tsx:290`) — half the drama is invisible; players may not realise the opponent shoots a seeded AI keeper. **Fix:** animate the opponent's most recent kick between yours, or explain in the PHASE_GUIDE penalties copy (live/match/[id]/page.tsx:849).

**13. Team page status badge "● AVAILABLE" is a vestigial mystery.**
Always-on green chip (`team/page.tsx:296-301`) left over from the retired stale/available system — never changes, means nothing. Similar vestige: result page's draw branch "GO AGAIN →" (result/page.tsx:138-141) unreachable since draws always route to pens (result/page.tsx:31). **Fix:** remove/repurpose the badge; drop the dead draw branch.

## P3 — Polish

**14. Season result page: duplicate/misleading next actions.**
"🏆 My Team" and "⚔️ Go Head to Head" both link to `/38-0/team` (`season/page.tsx:302-311`); guests' "Go Head to Head" is actually a sign-in link (season/page.tsx:320-324). **Fix:** point "Go Head to Head" at `/38-0/live` (signed-in); label the guest one honestly.

**15. "No re-spin" rule surfaces only after the spin.**
One-spin-per-squad commitment communicated only by the post-spin footer (`play/page.tsx:247-249`). **Fix:** static caption under the button: "One spin per pick — you draft from what you're dealt."

**16. `/38-0/leaderboard`'s "MY LEAGUES" card links to cross-game `/leagues`, not `/38-0/leagues`.**
`leaderboard/page.tsx:71`; also stale empty-state copy "Cloud leaderboards activate once the season is live" (leaderboard/page.tsx:87). **Fix:** point at `/38-0/leagues`; refresh copy.

**17. Code comments + YOURSCORE.md still claim a 3D penalty scene; the shipped scene is 2D.**
`PenaltyShootout.tsx:4-15` documents "PenaltyScene3D" but imports `PenaltyScene2D` (line 18, 262); no PenaltyScene3D file exists. YOURSCORE.md §5B sells "real-time 3D (React Three Fiber)". **Fix:** update docblock + YOURSCORE.md.

### What's notably good
WC Mastermind daily loop is the strongest surface: server-authoritative grading with no answer leakage (wc/page.tsx:254-278), clean one-run-per-day lock messaging with "Just Play" escape hatch (wc/page.tsx:493-516), edition strip for catch-up days, pending-amber answer states, coherent share loop. Live H2H PHASE_GUIDE coach, abandoned-match state, friend-lobby 45s fallback prompt, abandon-proof pens auto-settlement all well executed.

**Highest-leverage in order: #1 (restore or remove win-swap), #3 (skip button), #2 (guest quick match), #5 (loss framing), #6 (leaderboard taxonomy).** Items 1, 2, 5, 7, 17 also require YOURSCORE.md corrections.
